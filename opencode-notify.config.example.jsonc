{
  // 通知服务使用的 token
  "token": "your-notify-token",

  // 必填，替换成你自己的通知服务发送接口
  "apiUrl": "https://your-notify-host.example.com/api/v2/message/send",

  // 可选，通知标题里的后缀
  "title": "OpenCode",

  // 可选，超过这个长度会被截断
  "previewLimit": 30
}
